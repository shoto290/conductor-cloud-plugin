{
  "mcpServers": {
    "conductor-cloud": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": ["scripts/start.sh"],
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "CONDUCTOR_API_KEY": "${CONDUCTOR_API_KEY}",
        "CONDUCTOR_NODE": "${CONDUCTOR_NODE}"
      }
    }
  }
}
