# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.yarn/
.pnp.*
# dist/ is build output again. mcp.json now starts the server with `npx -y
# conductor-cloud-plugin`, so the checkout is never what runs — the npm tarball
# is, and `prepare` builds it at publish time.
dist/
build/
coverage/
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example

# Keys and secrets
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_ed25519*
secrets/
*.secret

# Conductor
.conductor/

# Claude Code — .claude/settings.json is committed, personal overrides are not
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
